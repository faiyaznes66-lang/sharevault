import {createClient} from '@supabase/supabase-js';
const url=import.meta.env.VITE_SUPABASE_URL||'';const anonKey=import.meta.env.VITE_SUPABASE_ANON_KEY||'';export const backendReady=Boolean(url&&anonKey);export const supabase=backendReady?createClient(url,anonKey):null;
export const auth={signUp:(email,password)=>supabase.auth.signUp({email,password}),signIn:(email,password)=>supabase.auth.signInWithPassword({email,password}),signOut:()=>supabase.auth.signOut(),session:()=>supabase.auth.getSession()};
export async function uploadFile(file,userId,title=''){if(!backendReady)throw new Error('Connect Supabase first.');if(file.size>104857600)throw new Error('Maximum file size is 100 MB.');const safe=file.name.replace(/[^a-zA-Z0-9._-]/g,'_');const path=`${userId}/${crypto.randomUUID()}-${safe}`;const{error:upErr}=await supabase.storage.from('user-files').upload(path,file,{contentType:file.type||'application/octet-stream',upsert:false});if(upErr)throw upErr;const slug=crypto.randomUUID().replaceAll('-','').slice(0,14);const{data,error}=await supabase.from('files').insert({owner_id:userId,slug,title:title||file.name,storage_path:path,mime_type:file.type||'application/octet-stream',size_bytes:file.size}).select().single();if(error){await supabase.storage.from('user-files').remove([path]);throw error}return data}
export async function myFiles(userId){const{data,error}=await supabase.from('files').select('*').eq('owner_id',userId).neq('status','deleted').order('created_at',{ascending:false});if(error)throw error;return data}
export async function fileAnalytics(){const{data,error}=await supabase.from('file_analytics').select('*');if(error)throw error;return data}
async function edgeFile(slug,event){if(!backendReady)throw new Error('Backend is not configured.');const res=await fetch(`${url}/functions/v1/file-access`,{method:'POST',headers:{'Content-Type':'application/json','apikey':anonKey},body:JSON.stringify({slug,event})});const data=await res.json();if(!res.ok)throw new Error(data.error||'Unable to access file');return data}
export const publicFile=slug=>edgeFile(slug,'view');export const downloadFile=slug=>edgeFile(slug,'download');
